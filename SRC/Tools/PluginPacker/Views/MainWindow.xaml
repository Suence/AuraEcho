<Window x:Class="PluginPacker.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:av="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:constants="clr-namespace:PluginPacker.Constants"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:viewmodels="clr-namespace:PluginPacker.ViewModels"
        Title="Plugin packer"
        Width="800"
        Height="480"
        av:DataContext="{av:DesignInstance Type=viewmodels:MainWindowViewModel}"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="av">
    <Grid>
        <ContentControl prism:RegionManager.RegionName="{x:Static constants:RegionNames.MainRegion}" />
    </Grid>
</Window>
