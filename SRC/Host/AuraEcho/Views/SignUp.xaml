<UserControl x:Class="AuraEcho.Views.SignUp"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cc="clr-namespace:AuraEcho.PluginContracts.Constants;assembly=AuraEcho.PluginContracts"
             xmlns:cm="clr-namespace:AuraEcho.Core.Models;assembly=AuraEcho.Core"
             xmlns:converters="clr-namespace:AuraEcho.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:AuraEcho.Core.Models;assembly=AuraEcho.Core"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:ui="http://ui.auraecho.com/"
             xmlns:viewmodels="clr-namespace:AuraEcho.ViewModels"
             d:Background="{DynamicResource Themes.Brushes.Background}"
             d:DataContext="{d:DesignInstance Type=viewmodels:SignUpViewModel}"
             d:DesignHeight="355"
             d:DesignWidth="535"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <Grid Margin="0,36,0,0">
        <Border Width="450"
                Height="350"
                Padding="32"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="#10FFFFFF"
                CornerRadius="8">
            <Grid>

                <StackPanel>
                    <TextBlock Margin="0,0,0,24"
                               FontSize="30"
                               Text="SIGN UP TO AURAECHO." />
                    <TextBox Height="40"
                             Margin="0,0,0,16"
                             Padding="16,0"
                             ui:ControlHelper.PlaceHolder="USERNAME"
                             FontSize="20"
                             Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" />
                    <PasswordBox Height="40"
                                 Padding="16,0"
                                 ui:ControlHelper.PlaceHolder="PASSWORD"
                                 ui:PasswordBoxHelper.Password="{Binding Password, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                 FontSize="20" />
                </StackPanel>
                <StackPanel VerticalAlignment="Bottom">

                    <Button Height="50"
                            Margin="0,0,0,8"
                            Command="{Binding SignUpCommand}"
                            Content="SIGN UP"
                            FontSize="20"
                            Style="{DynamicResource PrimaryButtonStyle}" />
                    <TextBlock>
                        <Hyperlink Command="{Binding NavigationToSignInCommand}">
                            ALREADY HAVE AN ACCOUNT? GO TO SIGN IN
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
