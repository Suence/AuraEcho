<UserControl x:Class="AuraEcho.UIToolkit.RegionDialog.ConfirmDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AuraEcho.UIToolkit.RegionDialog"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance Type=local:ConfirmDialogViewModel}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Background="#60000000"
             Foreground="#EFEFEF"
             mc:Ignorable="d">
    <Grid Width="320"
          Height="150"
          Background="#212121">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Height="24">
            <TextBlock Margin="4"
                       VerticalAlignment="Center"
                       d:Text="Dialog Title"
                       Text="{Binding Parameter.Title}" />
            <Button Padding="12,0"
                    HorizontalAlignment="Right"
                    Command="{Binding CloseCommand}"
                    Content="X" />
        </Grid>
        <TextBlock Grid.Row="1"
                   Margin="0,0,0,10"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   d:Text="Are you sure?"
                   Text="{Binding Parameter.Message}"
                   TextWrapping="Wrap" />
        <StackPanel Grid.Row="2"
                    Margin="8"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Margin="0,0,8,0"
                    Padding="8,4"
                    Command="{Binding CancelCommand}"
                    Content="{Binding Parameter.CancelText}" />
            <Button Padding="8,4"
                    Command="{Binding OkCommand}"
                    Content="{Binding Parameter.ConfirmText}" />
        </StackPanel>
    </Grid>
</UserControl>
