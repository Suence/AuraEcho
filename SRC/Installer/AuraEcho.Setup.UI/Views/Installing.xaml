<UserControl x:Class="AuraEcho.Setup.UI.Views.Installing"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:viewmodels="clr-namespace:AuraEcho.Setup.UI.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:InstallingViewModel}"
             d:DesignHeight="500"
             d:Foreground="White"
             d:DesignWidth="750"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Loaded="UserControl_Loaded"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Storyboard x:Key="ProgressStorybard">
            <DoubleAnimation Storyboard.TargetName="InstallProgressBar"
                             Storyboard.TargetProperty="Value"
                             Duration="0:0:2" />
        </Storyboard>
    </UserControl.Resources>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding InstallCommand}" />
        </i:EventTrigger>
        <i:PropertyChangedTrigger Binding="{Binding Progress}">
            <i:CallMethodAction MethodName="BeginProgressStoryboard" TargetObject="{Binding RelativeSource={RelativeSource AncestorType=UserControl}}" />
        </i:PropertyChangedTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" />
        <StackPanel Grid.Row="1" Margin="24">
            <DockPanel Margin="0,0,0,8" LastChildFill="True">
                <TextBlock d:Text="10%"
                           DockPanel.Dock="Right"
                           Text="{Binding ElementName=InstallProgressBar, Path=Value, StringFormat={}{0:N0}%}" />
                <TextBlock d:Text="正在安装，请稍后..."
                           DockPanel.Dock="Left"
                           Margin="0, 0, 20, 0"
                           FontSize="14"
                           TextTrimming="CharacterEllipsis"
                           Text="{Binding Message}" />
            </DockPanel>
            <ProgressBar x:Name="InstallProgressBar"
                         Height="4"
                         Margin="0,0,0,16"
                         d:Value="10"
                         Maximum="100"
                         Minimum="0" />
            <Button Width="75"
                    Height="36"
                    HorizontalAlignment="Right"
                    Command="{Binding CancelCommand}"
                    Content="取消"
                    FontSize="14" />
        </StackPanel>
    </Grid>
</UserControl>
